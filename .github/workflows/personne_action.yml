name: action Personnalize
on: [push]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Dire bounjor avec mon actionn
        uses: yssnbh/github-actions-personne@main
        with:
          nom: "bouhroz"
          langue: "es"